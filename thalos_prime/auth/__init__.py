"""Authentication package for Thalos Prime.

Purpose: Provides authentication and authorisation primitives for the
Thalos Prime API server (API key verification, JWT validation, etc.).

Interfaces: None exported at this time.

Lifecycle: N/A — no runtime components at current operational scope.

No runtime implementation required for current operational scope.
The system operates as a deterministic Library of Babel pipeline; the
API layer uses a simple API-key check in the admin routes directly.
"""
